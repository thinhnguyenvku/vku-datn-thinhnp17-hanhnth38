{
	"devDependencies": {
		"npm-run-all": "^4.1.5"
	},
	"scripts": {
		"dev:frontend": "npm run dev --prefix ./frontend",
		"dev:backend": "npm run dev --prefix ./backend",
		"dev:ai": "PORT=5178 npm run dev --prefix ./ai-interview",
		"dev": "npm-run-all --parallel dev:frontend dev:backend dev:ai"
	}
}
